const accountsSchema = require('./accounts');
const activeKeyBwmsSchema = require('./activeKeyBwm');
const logsBwmsSchema = require('./logs');

module.exports = {
    accountsSchema,
    activeKeyBwmsSchema,
    logsBwmsSchema,
};